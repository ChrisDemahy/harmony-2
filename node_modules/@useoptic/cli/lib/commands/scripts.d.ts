import { Command } from '@oclif/command';
import { IOpticScript } from '@useoptic/cli-config/build';
export default class Scripts extends Command {
    static description: string;
    static args: {
        name: string;
        required: boolean;
    }[];
    static flags: {
        install: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    run(): Promise<void>;
    executeScript(script: IOpticScript): Promise<void>;
}
