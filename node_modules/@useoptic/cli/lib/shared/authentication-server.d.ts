/// <reference types="node" />
import { EventEmitter } from 'events';
import { IUser, IUserCredentials } from '@useoptic/cli-config';
interface ICredentialsServerConfig {
    port: number;
}
export declare const loginBaseUrl: string;
export declare const tokenReceivedEvent: string;
declare class CredentialsServer {
    private server;
    events: EventEmitter;
    start(config: ICredentialsServerConfig): Promise<void>;
    stop(): Promise<void>;
}
export declare function ensureCredentialsServerStarted(overridePort?: number | undefined): Promise<{
    server: CredentialsServer;
    port: number;
}>;
export declare function setCredentials(credentials: IUserCredentials): Promise<void>;
export declare function deleteCredentials(): Promise<void>;
export declare function getCredentials(): Promise<IUserCredentials | null>;
export declare function getUserFromCredentials(credentials: IUserCredentials): Promise<IUser>;
export {};
