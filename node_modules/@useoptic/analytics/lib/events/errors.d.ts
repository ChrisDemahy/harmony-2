import { RegisteredEvent } from '../interfaces/RegisterEvent';
import * as Joi from '@hapi/joi';
import 'joi-extract-type';
export declare const RequirementForDiffsToHaveASuggestionFailed: RegisteredEvent<Joi.extractMap<{
    diff: Joi.BoxStringSchema<Joi.Box<string, true>>;
}>>;
export declare const JavascriptErrorDetectedInFrontend: RegisteredEvent<Joi.extractMap<{
    message: Joi.BoxStringSchema<Joi.Box<string, true>>;
    stack: Joi.BoxStringSchema<Joi.Box<string, true>>;
}>>;
