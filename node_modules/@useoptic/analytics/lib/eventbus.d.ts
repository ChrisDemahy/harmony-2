/// <reference types="node" />
import { EventEmitter } from 'events';
import { ClientContext, TrackingEventBase } from './interfaces/TrackingEventBase';
export declare function newAnalyticsEventBus(getContext: (batchId: string) => Promise<ClientContext>): AnalyticsEventBus;
export interface AnalyticsEventBus {
    eventEmitter: EventEmitter;
    emit: (...events: TrackingEventBase<any>[]) => void;
    listen: (callback: (e: TrackingEventBase<any>) => void) => void;
}
