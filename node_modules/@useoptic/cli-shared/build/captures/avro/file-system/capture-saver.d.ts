import { IGroupingIdentifiers, IHttpInteraction } from '@useoptic/domain-types';
import { ICaptureSaver } from '../../../index';
interface IFileSystemCaptureSaverConfig {
    captureBaseDirectory: string;
    captureId: string;
}
export declare class CaptureSaver implements ICaptureSaver {
    private config;
    private batcher;
    private tracking;
    private batchCount;
    private interactionsReceivedCount;
    private interactionsSavedCount;
    constructor(config: IFileSystemCaptureSaverConfig);
    init(): Promise<void>;
    onBatch(groupingIdentifiers: IGroupingIdentifiers, batchId: string, items: IHttpInteraction[], outputDirectory: string): Promise<void>;
    save(sample: IHttpInteraction): Promise<void>;
    cleanup(): Promise<void>;
}
export {};
//# sourceMappingURL=capture-saver.d.ts.map