import { IHttpInteraction } from '@useoptic/domain-types';
import { ICaptureSaver } from '../../../index';
export interface ISaasCaptureSaverConfig {
    orgId: string;
    agentGroupId: string;
    agentId: string;
    captureId: string;
    launchTokenString: string;
    baseUrl: string;
}
declare class CaptureSaver implements ICaptureSaver {
    private config;
    private saasClient;
    private batcher;
    private throttler;
    private batchCount;
    private isStopping;
    constructor(config: ISaasCaptureSaverConfig);
    init(): Promise<void>;
    save(sample: IHttpInteraction): Promise<void>;
    private saveBatch;
    cleanup(): Promise<void>;
}
export { CaptureSaver };
//# sourceMappingURL=capture-saver.d.ts.map