export interface ICliDaemonConfig {
    lockFilePath: string;
    cloudApiBaseUrl: string;
}
declare class CliDaemon {
    private config;
    private apiServer;
    private releaseLock;
    constructor(config: ICliDaemonConfig);
    start(): Promise<{
        port: number;
    }>;
    acquireInstanceLock(): Promise<void>;
    releaseInstanceLock(): Promise<void>;
    startApiServer(): Promise<{
        port: number;
    }>;
    stopApiServer(): Promise<void>;
    stop(): Promise<void>;
}
export { CliDaemon };
//# sourceMappingURL=daemon.d.ts.map