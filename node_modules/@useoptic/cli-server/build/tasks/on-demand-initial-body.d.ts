/// <reference types="node" />
import { EventEmitter } from 'events';
import { ILearnedBodies } from '@useoptic/cli-shared/build/diffs/initial-types';
export interface IInitialBodyManagerConfig {
    pathId: string;
    method: string;
    captureId: string;
    events: any;
    captureBaseDirectory: string;
}
export declare class OnDemandInitialBody {
    readonly events: EventEmitter;
    private child;
    readonly id: string;
    private readonly config;
    private finished;
    constructor(config: IInitialBodyManagerConfig);
    private lastProgress;
    run(): Promise<ILearnedBodies>;
    private paths;
    stop(): Promise<void>;
}
//# sourceMappingURL=on-demand-initial-body.d.ts.map